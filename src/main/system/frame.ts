import { ModuleDefinition } from '../types/index.js';

export const Frame: ModuleDefinition<{}, any> = {
    label: 'Frame',
    resizeMode: 'all',
    params: {},
    result: {
        schema: { type: 'any' },
    },
};
