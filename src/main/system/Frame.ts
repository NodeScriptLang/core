import { ModuleDefinition } from '../types/index.js';

export const Frame: ModuleDefinition<{}, any> = {
    moduleName: '@system/Frame',
    version: '0.0.0',
    label: 'Frame',
    resizeMode: 'all',
    params: {},
    result: {
        schema: { type: 'any' },
    },
};
